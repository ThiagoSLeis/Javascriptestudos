//console.log("Minha idade atual é: ", idade)
// Usando o let ou const o código quebra, pois a varíavel não pode ser referenciada após a criação dela

let idade = 37

// variável global não pode ser referencida antes da declaração
// var pode
// let, const não podem ser referenciadas depois da inicialização

console.log("Minha idade atual depois de declarar: ", idade)

/* 
(), * / + -
> < >= <= == === != !==
NOT (!), AND (&&), OR (||)
*/